<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:controls="clr-namespace:ioSenderTouch.Controls"
                    xmlns:grblCore="clr-namespace:ioSenderTouch.GrblCore">
    <controls:IsAxisVisibleConverter x:Key="IsAxisVisibleConverter" />
    <Style x:Key="SmallXButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Grid Background="{TemplateBinding Background}">
                        <ContentPresenter HorizontalAlignment="Center"/>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="YAxisVisibleStyle">
        <Setter Property="Control.Visibility">
            <Setter.Value>
                <MultiBinding Converter="{StaticResource IsAxisVisibleConverter}">
                    <Binding Path="AxisEnabledFlags" />
                    <Binding>
                        <Binding.Source>
                            <grblCore:AxisFlags>Y</grblCore:AxisFlags>
                        </Binding.Source>
                    </Binding>
                </MultiBinding>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="ZAxisVisibleStyle">
        <Setter Property="Control.Visibility">
            <Setter.Value>
                <MultiBinding Converter="{StaticResource IsAxisVisibleConverter}">
                    <Binding Path="AxisEnabledFlags" />
                    <Binding>
                        <Binding.Source>
                            <grblCore:AxisFlags>Z</grblCore:AxisFlags>
                        </Binding.Source>
                    </Binding>
                </MultiBinding>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="AAxisVisibleStyle">
        <Setter Property="Control.Visibility">
            <Setter.Value>
                <MultiBinding Converter="{StaticResource IsAxisVisibleConverter}">
                    <Binding Path="AxisEnabledFlags" />
                    <Binding>
                        <Binding.Source>
                            <grblCore:AxisFlags>A</grblCore:AxisFlags>
                        </Binding.Source>
                    </Binding>
                </MultiBinding>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="BAxisVisibleStyle">
        <Setter Property="Control.Visibility">
            <Setter.Value>
                <MultiBinding Converter="{StaticResource IsAxisVisibleConverter}">
                    <Binding Path="AxisEnabledFlags" />
                    <Binding>
                        <Binding.Source>
                            <grblCore:AxisFlags>B</grblCore:AxisFlags>
                        </Binding.Source>
                    </Binding>
                </MultiBinding>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="CAxisVisibleStyle">
        <Setter Property="Control.Visibility">
            <Setter.Value>
                <MultiBinding Converter="{StaticResource IsAxisVisibleConverter}">
                    <Binding Path="AxisEnabledFlags" />
                    <Binding>
                        <Binding.Source>
                            <grblCore:AxisFlags>C</grblCore:AxisFlags>
                        </Binding.Source>
                    </Binding>
                </MultiBinding>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="UAxisVisibleStyle">
        <Setter Property="Control.Visibility">
            <Setter.Value>
                <MultiBinding Converter="{StaticResource IsAxisVisibleConverter}">
                    <Binding Path="AxisEnabledFlags" />
                    <Binding>
                        <Binding.Source>
                            <grblCore:AxisFlags>U</grblCore:AxisFlags>
                        </Binding.Source>
                    </Binding>
                </MultiBinding>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="VAxisVisibleStyle">
        <Setter Property="Control.Visibility">
            <Setter.Value>
                <MultiBinding Converter="{StaticResource IsAxisVisibleConverter}">
                    <Binding Path="AxisEnabledFlags" />
                    <Binding>
                        <Binding.Source>
                            <grblCore:AxisFlags>V</grblCore:AxisFlags>
                        </Binding.Source>
                    </Binding>
                </MultiBinding>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="WAxisVisibleStyle">
        <Setter Property="Control.Visibility">
            <Setter.Value>
                <MultiBinding Converter="{StaticResource IsAxisVisibleConverter}">
                    <Binding Path="AxisEnabledFlags" />
                    <Binding>
                        <Binding.Source>
                            <grblCore:AxisFlags>W</grblCore:AxisFlags>
                        </Binding.Source>
                    </Binding>
                </MultiBinding>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>