<UserControl x:Class="ioSenderTouch.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:system="clr-namespace:System;assembly=mscorlib"
             xmlns:local="clr-namespace:ioSenderTouch.Controls"
             xmlns:viewModels="clr-namespace:ioSenderTouch.ViewModels"
             d:DesignHeight="1080" d:DesignWidth="1920"
             d:DataContext="{d:DesignInstance Type=viewModels:GrblViewModel, IsDesignTimeCreatable=True}"
             mc:Ignorable="d"
             Focusable="True">
    <UserControl.Resources>
        <local:BoolToNotVisibleConverter x:Key="BoolToNotVisibleConverter"/>
        <local:BoolToVisibleConverter x:Key="BoolToVisibleConverter" />
        <local:LogicalNotConverter x:Key="LogicalNotConverter" />
    </UserControl.Resources>
    <DockPanel Name="DockPanelMain">
        <Border Height="35"  BorderBrush="Black" BorderThickness="1" DockPanel.Dock="Top">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="Job" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" Command="{Binding HomeViewModel.ChangeView}" 
                        CommandParameter="RenderView"  Width="140" Margin="1,0,1,0" ></Button>
                <Button Content="Probe" IsEnabled="{Binding IsJobRunning, Converter={StaticResource LogicalNotConverter }, Mode=TwoWay}" 
                        Visibility="{Binding  Path= HasProbing,Converter={StaticResource BoolToVisibleConverter}}" HorizontalContentAlignment="Center"
                        VerticalContentAlignment="Center" Command="{Binding HomeViewModel.ChangeView}" CommandParameter="ProbingView"  Width="140" Margin="1,0,1,0" ></Button>
                <Button Content="SD Card" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" Command="{Binding HomeViewModel.ChangeView}" 
                        CommandParameter="SDCardView" Visibility="{Binding  Path= HasSDCard,Converter={StaticResource BoolToVisibleConverter}}"  
                        IsEnabled="{Binding IsJobRunning, Converter={StaticResource LogicalNotConverter}}" 
                        Width="140" Margin="1,0,1,0" ></Button>
                <Button Content="Offset" IsEnabled="{Binding IsJobRunning, Converter={StaticResource LogicalNotConverter}}" 
                        HorizontalContentAlignment="Center" VerticalContentAlignment="Center" Command="{Binding HomeViewModel.ChangeView}" 
                        CommandParameter="offsetView"
                        Width="140" Margin="1,0,1,0" ></Button>
                <Button Content="Utility" IsEnabled="{Binding IsJobRunning, Converter={StaticResource LogicalNotConverter}}" 
                        HorizontalContentAlignment="Center" VerticalContentAlignment="Center" Command="{Binding HomeViewModel.ChangeView}"
                        CommandParameter="utilityView"
                        Width="140" Margin="1,0,1,0" ></Button>
                <Button Content="Tools" IsEnabled="{Binding IsJobRunning, Converter={StaticResource LogicalNotConverter}}" 
                        HorizontalContentAlignment="Center" VerticalContentAlignment="Center" Command="{Binding HomeViewModel.ChangeView}" CommandParameter="toolsView"
                        Visibility="{Binding  Path= HasToolTable,Converter={StaticResource BoolToVisibleConverter}}" Width="140" Margin="1,0,1,0" ></Button>
                <Button Content="Grbl Settings" IsEnabled="{Binding IsJobRunning, Converter={StaticResource LogicalNotConverter}}" 
                        HorizontalContentAlignment="Center" VerticalContentAlignment="Center" Command="{Binding HomeViewModel.ChangeView}" CommandParameter="grblSettingsView"
                        Width="140" Margin="1,0,1,0"  ></Button>
                <Button Content="App Settings" IsEnabled="{Binding IsJobRunning,  Converter={StaticResource LogicalNotConverter}}" 
                        HorizontalContentAlignment="Center" VerticalContentAlignment="Center" Command="{Binding HomeViewModel.ChangeView}" CommandParameter="appSettingsView"
                        Width="140" Margin="1,0,1,0" ></Button>
                
            </StackPanel>
        </Border>
        <Border Name="MainControlBorder" Height="320"  BorderBrush="Black" BorderThickness="1" DockPanel.Dock="Bottom">
            <StackPanel Name="MainStackPanel"  Orientation="Horizontal" Margin="0,0,0,0" >
                <local:MDIControl  x:Name="MdiControl" HorizontalAlignment="Left" VerticalAlignment="Bottom" Width="415" Height="Auto" Margin="5,3,0,5"/>
                <local:DROControl x:Name="DRO" HorizontalAlignment="Left" VerticalAlignment="Stretch" Margin="3,3,0,5"  VerticalContentAlignment="Stretch"/>
                <local:WorkParametersControlTouch x:Name="workParametersControl"  Margin="3,3,0,5" 
                                                     Height="Auto" Width="256" VerticalAlignment="Stretch" HorizontalAlignment="Stretch"/>

                <local:FeedControl  x:Name="feedControl"  HorizontalAlignment="Center"  VerticalAlignment="Stretch"  Margin="3,3,0,5" Width="260" Height="Auto" />
                <local:JogControl x:Name="jogControl" Margin="3,3,0,5" Height="Auto" HorizontalAlignment="Stretch" Width="395" VerticalAlignment="Stretch" />
                <local:SpindleControl x:Name="spindleControl" HorizontalAlignment="Stretch" Margin="3,3,3,5" Width="262"  VerticalAlignment="Stretch"/>
            </StackPanel>
        </Border>

        <Grid DockPanel.Dock="Left" Width="420">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="30"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="0" VerticalAlignment ="Top" Height="35"  Grid.RowSpan="2" >
                <Button  Name="btnShowConsole" Content="{Binding HomeViewModel.ConsoleModeText}"
                         Command="{Binding HomeViewModel.SwitchConsoleCommand}"  
                         Visibility="{Binding Path=IsFileLoaded, Converter= {StaticResource BoolToVisibleConverter}}"></Button>
            </StackPanel>
            <local:ConsoleControl Grid.Row="1" x:Name="ConsoleDisplayControl" 
                                  Visibility="{Binding Path= HomeViewModel.ShowGCodeConsole, Converter= {StaticResource BoolToNotVisibleConverter}}"
                                  Margin="2,5,0,0" Grid.ColumnSpan="2" />
            <local:GCodeListControl Grid.Row="1" x:Name ="CodeListControl" Margin="2,5,0,0" 
                                    Visibility="{Binding Path= HomeViewModel.ShowGCodeConsole, Converter= {StaticResource BoolToVisibleConverter}}" 
                                    Grid.ColumnSpan="2"/>
            <!--<controls:MDIControl Grid.Row="2" x:Name="MdiControl" HorizontalAlignment="Left" VerticalAlignment="Bottom" Width="Auto" Height="Auto" Margin="5,3,0,5"/>-->
        </Grid>

        <Border Name="RightMenuBorder" BorderBrush="Black" BorderThickness="1" Width="250" DockPanel.Dock="Right">
            <Grid Name ="RightStackPanel">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"></RowDefinition>
                    <RowDefinition Height="Auto"></RowDefinition>
                    <RowDefinition Height="*" ></RowDefinition>
                </Grid.RowDefinitions>
                <!--<local:JobControl x:Name="GCodeSender" Margin="3,3,0,5" Width="244" VerticalAlignment="Top"/>-->
                <local:CoolantControl Grid.Row="0" x:Name="coolantControl" Margin="3,3,3,3"   VerticalAlignment="Top" Height="Auto" Width="Auto"/>
                <local:UtilityMacroControl Grid.Row="1" Margin="3,3,3,3" HorizontalAlignment="Stretch"  VerticalAlignment="Stretch"></local:UtilityMacroControl>
            </Grid>
        </Border>
        <Border  BorderBrush="Black" BorderThickness="1"  DockPanel.Dock="Right">
            <ContentControl Name="CenterView" Content="{Binding HomeViewModel.View}" 
                            HorizontalAlignment="Stretch" VerticalAlignment="Stretch"></ContentControl>
        </Border>
    </DockPanel>
</UserControl>
