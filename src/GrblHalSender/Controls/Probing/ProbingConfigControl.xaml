<UserControl x:Class="GrblHalSender.Controls.Probing.ProbingConfigControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="120" d:DesignWidth="300">
    <GroupBox x:Uid="grp_probeConfig" Header="Probing" HorizontalAlignment="Left" VerticalAlignment="Top" Width="300" >
        <StackPanel HorizontalAlignment="Stretch"  VerticalAlignment="Stretch" >
            <CheckBox x:Uid="lbl_chkProbeConnected" Content="Manually validate probe is connected" HorizontalContentAlignment="Left" HorizontalAlignment="Left" 
                      Margin="0,0,0,0" IsChecked="{Binding Path=Probing.ValidateProbeConnected, Mode=TwoWay}" ToolTip="A dialog will be shown until the probe is asserted or the dialog is cancelled."/>
            <CheckBox x:Uid="lbl_chkProbeStatus" Content="Check probe status before start G59.3" HorizontalContentAlignment="Left" HorizontalAlignment="Left" 
                      Margin="0,0,0,0" IsChecked="{Binding Path=Probing.CheckProbeStatus, Mode=TwoWay}" ToolTip="An alarm will be generated by the controller if not enabled and the probe is asserted." Height="27"/>
        </StackPanel>
    </GroupBox>
</UserControl>
